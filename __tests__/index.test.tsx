// import { render, screen } from "@testing-library/react";
import "@testing-library/jest-dom";

// import Home from "@/app/page";

it.todo("the user can add an item to the list");
it.todo("the user can edit an item in the list");
it.todo("the user can mark an item as done in the list");
it.todo("automatically save the time when a new item is created");
it.todo('shows a "last updated" time when editing an item in the list');
it.todo("the user can check off an item, which archives it");
it.todo("the user can view previously archived items");
